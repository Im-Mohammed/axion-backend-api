services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./service_account.json:/backend/service_account.json:ro
    restart: unless-stopped